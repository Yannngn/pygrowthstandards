repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    # Ruff version.
    rev: v0.12.2
    hooks:
      # Run the linter.
      - id: ruff-check
        args: ["--fix", "--line-length=144"]
      # Run the formatter.
      - id: ruff-format
